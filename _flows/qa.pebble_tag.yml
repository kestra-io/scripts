id: pebble_tag
namespace: qa

variables:
  trigger_var: "{{ execution.startDate | date('yyyy-MM-dd') }}"

tasks:
  - id: parse_date
    type: io.kestra.core.tasks.debugs.Return
    format: |
      {% set header = "Today is" %}

      {{ header ~ ' ' ~ render(vars.trigger_var) }}